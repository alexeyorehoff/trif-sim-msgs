# Trigger planning on all queued toolpaths.
# Returns the full planned result including FK, IK, collision, manipulability.
string planning_group  # optional: load/switch to this group before planning
bool   clear_after     # if true, clear the queue after successful planning
---
PlannedToolpath result
